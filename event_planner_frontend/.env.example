# PUBLIC_INTERFACE
# REACT_APP_BACKEND_URL: Base URL to your backend API. If omitted, the app will call the same origin.
# Example:
# REACT_APP_BACKEND_URL=http://localhost:3001
REACT_APP_BACKEND_URL=
